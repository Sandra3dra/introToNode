// import { builtinModules } from "module"

const config = {
    uname: "root",
    upass: "root", //windows leave this blank
    port: 8889,  //3306 for win and linux
    host: "localhost",
    db: "bizcard"
}

module.exports = config;